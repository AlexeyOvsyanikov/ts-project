/* eslint-disable no-bitwise */
/* eslint-disable no-multi-spaces */
/* eslint-disable no-plusplus */
// const source = [1, 2, 3, 4, 5, 6];
/**
 *
 * Дан произвольный массив с числами и заданное число.
 * Нужно найти все пары чисел в массиве, которые в сумме дают заданное.
 * Должно работать за O(N), где N - размер массива.
 *
 */
const source = [1, 5, 2, 6, 5, 4];

// function getPairs(src: number[], key: number): number[][] {
//   const { length } = src;

//   const result: number[][] = [];

//   for (let i = 0, j = length - 1; i < length; i++, j--) {
//     const left = src[i];
//     const right = src[j];

//     if (left + right === key) {
//       result.push([left, right]);
//     }
//   }

//   return result;
// }

// const pairs = getPairs(source, 7);

// function getPairs(src: number[], key: number): number[][] {}

// const pairs = getPairs(source, 7);
// console.log('pairs: ', pairs);
